# P1.5 水滴球体优化 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将水滴球体优化为清澈水球 + 波澜呼吸基态，降低墨色密度、简化表面波纹、取消呼吸开关。

**Architecture:** 4 个文件独立修改——Shader 改颜色和顶点波、WaterSphere.tsx 改呼吸逻辑和 opacity 映射、types.ts 改默认值和移除字段、StageControls.tsx 移除复选框。

**Tech Stack:** TypeScript + GLSL + React Three Fiber

---

## 文件总览

| 文件 | 操作 |
|------|------|
| `src/renderer/components/pet-v2/shaders/waterSphereMaterial.ts` | 修改 — 5 个颜色 uniform 默认值 + computeDisplacement 重建 |
| `src/renderer/components/pet-v2/WaterSphere.tsx` | 修改 — 取消 breathEnabled 分支 + 缩放幅值减半 + opacity 映射改起点 + breathAmp 固定 0.35 |
| `src/renderer/components/pet-v2/types.ts` | 修改 — 5 个默认值改值 + 移除 breathEnabled 字段 |
| `src/renderer/components/pet-v2/StageControls.tsx` | 修改 — 移除呼吸开关 UI |

---

## Task 1: types.ts — 默认值更新 + 移除 breathEnabled

**Files:**
- Modify: `src/renderer/components/pet-v2/types.ts`

- [ ] **Step 1: 更新默认值并从接口移除 breathEnabled**

在 [types.ts](file:///d:/Projects/ai-writing-coach/desktop-pet/src/renderer/components/pet-v2/types.ts) 中，三处修改：

**① WaterSphereParams 接口** — 第 12 行删除 `breathEnabled: boolean`：

```typescript
export interface WaterSphereParams {
  radius: number
  transparency: number
  fresnelStrength: number
  highlightStrength: number
  refractionStrength: number
  inkDensity: number
  inkSpread: number
  backlightStrength: number
}
```

**② DEFAULT_WATER_SPHERE_PARAMS** — 更新 5 个值 + 删除 `breathEnabled: false`：

```typescript
export const DEFAULT_WATER_SPHERE_PARAMS: WaterSphereParams = {
  radius: 1.45,
  transparency: 0.18,
  fresnelStrength: 0.65,
  highlightStrength: 0.75,
  refractionStrength: 0.55,
  inkDensity: 0.22,
  inkSpread: 0.18,
  backlightStrength: 0.48,
}
```

**③ V2StageParams 和 DEFAULT_V2_STAGE_PARAMS 不做改动。**

- [ ] **Step 2: 运行类型检查确认字段移除后无引用错误**

```bash
npm run typecheck
```

预期会有 downstream 类型错误（WaterSphere.tsx 和 StageControls.tsx 仍引用 `breathEnabled`），这些在后续 Task 修复。

---

## Task 2: waterSphereMaterial.ts — Shader 颜色 + 顶点波重建

**Files:**
- Modify: `src/renderer/components/pet-v2/shaders/waterSphereMaterial.ts`

- [ ] **Step 1: 更新 5 个颜色 uniform 默认值**

在 [waterSphereMaterial.ts](file:///d:/Projects/ai-writing-coach/desktop-pet/src/renderer/components/pet-v2/shaders/waterSphereMaterial.ts) 的 `createInkSphereMaterial` 函数中：

| 行号 | 当前 | 改为 |
|------|------|------|
| 22 | `new THREE.Color('#4a6078')` | `new THREE.Color('#d8eef8')` |
| 24 | `new THREE.Color('#0a0f16')` | `new THREE.Color('#0a1018')` |
| 30 | `new THREE.Color('#2a6090')` | `new THREE.Color('#78c8e8')` |
| 39 | `value: 0.7` | `value: 0.22` |
| 40 | `value: 0.55` | `value: 0.18` |
| 42 | `value: 0.78` | `value: 0.18` |

修改后该段为：

```typescript
      // 淡墨色（半透明灰蓝，右下区域）
      uLightInk: { value: new THREE.Color('#d8eef8') },
      // 浓墨色（实心黑墨，左上区域）
      uDarkInk: { value: new THREE.Color('#0a1018') },
      // 高光颜色
      uSpecColor: { value: new THREE.Color('#e8f6ff') },
      // Fresnel 边缘光颜色
      uFresnelColor: { value: new THREE.Color('#6098b8') },
      // 背光透射颜色
      uBacklightColor: { value: new THREE.Color('#78c8e8') },

      // ---- 参数 ----
      uAmbient: { value: 0.08 },
      uDiffuseStrength: { value: 0.78 },
      uSpecPower: { value: 180.0 },
      uSpecStrength: { value: 0.62 },
      uFresnelPower: { value: 5.5 },
      uFresnelStrength: { value: 0.14 },
      uInkDensity: { value: 0.22 },
      uInkSpread: { value: 0.18 },
      uBacklightStrength: { value: 0.48 },
      uOpacity: { value: 0.18 },
```

- [ ] **Step 2: 重建 computeDisplacement（10 波 → 3 波）**

将 [waterSphereMaterial.ts](file:///d:/Projects/ai-writing-coach/desktop-pet/src/renderer/components/pet-v2/shaders/waterSphereMaterial.ts#L65-L81) 的 `computeDisplacement` 函数整个替换为：

```glsl
      float computeDisplacement(vec3 dir) {
        // 波1 — 大尺度缓慢波澜（主导）
        float wave1 = sin(dir.y * 1.6 + uTime * 0.35) * 0.012;

        // 波2 — 横向缓慢涟漪
        float wave2 = sin(dir.x * 2.2 + uTime * 0.42) * cos(dir.z * 1.8 - uTime * 0.38) * 0.008;

        // 波3 — 极细微高频涟漪（仅在最表层）
        float wave3 = sin(dir.x * 5.5 + uTime * 1.1) * cos(dir.y * 5.2 - uTime * 0.9) * 0.003;

        return (wave1 + wave2 + wave3) * uBreathAmplitude;
      }
```

同时删除 `hash` 函数（56-59 行，不再使用）。

---

## Task 3: WaterSphere.tsx — 呼吸逻辑 + opacity + 缩放

**Files:**
- Modify: `src/renderer/components/pet-v2/WaterSphere.tsx`

- [ ] **Step 1: 修改 opacity 计算起点**

第 99 行改为：

```typescript
const opacity = 0.18 + params.transparency * 0.52  // 0.18 ~ 0.70
```

- [ ] **Step 2: 取消 breathEnabled 分支，统一呼吸基态**

将第 88-151 行（`useFrame` 回调）中的 if/else 分支替换为统一逻辑：

```typescript
  useFrame((state) => {
    if (!sphereRef.current || !material) return

    // 滑块 → shader 参数映射
    const specPower = 30 + params.refractionStrength * 320 // 30 ~ 350
    const specStr = params.highlightStrength * 0.78 // 0 ~ 0.78
    const fresnelPower = 2.5 + params.fresnelStrength * 6.0 // 2.5 ~ 8.5
    const fresnelStr = 0.04 + params.fresnelStrength * 0.28 // 0.04 ~ 0.32
    const inkDensity = params.inkDensity // 0 ~ 1
    const inkSpread = params.inkSpread // 0 ~ 1
    const backlight = params.backlightStrength // 0 ~ 1
    const opacity = 0.18 + params.transparency * 0.52 // 0.18 ~ 0.70

    const elapsed = state.clock.getElapsedTime()

    // 液体异步多轴缩放 — 默认基态呼吸
    const t = elapsed
    const scaleX = 1 + Math.sin(t * 0.75 + 0.0) * 0.010
    const scaleY = 1 + Math.sin(t * 1.1 + 1.4) * 0.012
    const scaleZ = 1 + Math.sin(t * 0.92 + 2.8) * 0.008
    sphereRef.current.scale.set(scaleX, scaleY, scaleZ)

    // 顶点级表面波澜 — 极弱幅度
    const breathAmp = 0.35
    updateInkSphereUniforms(
      material,
      specPower,
      specStr,
      fresnelPower,
      fresnelStr,
      inkDensity,
      inkSpread,
      backlight,
      opacity,
      elapsed,
      breathAmp
    )

    sphereRef.current.rotation.y = Math.sin(elapsed * 0.18) * 0.08
  })
```

---

## Task 4: StageControls.tsx — 移除呼吸开关

**Files:**
- Modify: `src/renderer/components/pet-v2/StageControls.tsx`

- [ ] **Step 1: 删除呼吸开关 UI**

删除第 151-158 行：

```tsx
      <label className="v2-stage-controls__check">
        <input
          type="checkbox"
          checked={params.sphere.breathEnabled}
          onChange={(e) => updateSphere('breathEnabled', e.target.checked)}
        />
        开启呼吸动画
      </label>
```

同时更新底部 note 文字（第 160-164 行），将：

```tsx
        Lambert + 背光透射 + Blinn-Phong + Fresnel
        <br />
        墨色密度场模拟左上浓墨/右下淡灰色。
```

改为：

```tsx
        Lambert + 背光透射 + Blinn-Phong + Fresnel
        <br />
        清澈水球 + 墨核心模式。呼吸为默认基态。
```

---

## Task 5: 完整检查

- [ ] **Step 1: Prettier**

```bash
npx prettier --write src/
```

- [ ] **Step 2: Typecheck**

```bash
npm run typecheck
```

Expected: 0 errors

- [ ] **Step 3: Lint**

```bash
npm run lint
```

Expected: 0 errors

- [ ] **Step 4: 预览验证**

```bash
npx vite --config preview/vite.config.ts --host 127.0.0.1 --port 5175
```

打开 `http://127.0.0.1:5175/`，验证：
- 球体清澈透亮，外层接近透明水体
- 表面只有缓慢波澜，无密集褶皱
- 呼吸始终开启，呼吸开关已消失
- 控制面板其他滑块正常工作
- 背景模式切换仍生效

---

## 任务依赖

```
Task 1 (types) ──→ Task 3 (WaterSphere) ──→ Task 4 (StageControls)
                                                    │
Task 2 (Shader) ────────────────────────────────────┘
                                                    │
                                                    ▼
                                              Task 5 (检查)
```

Tasks 1+2 互不依赖可并行；Tasks 3+4 依赖 Task 1（因为类型引用）。

---

## 风险

- `uOpacity` 默认从 0.78 降到 0.18 后球体可能过于透明，但可通过控制面板 `球体不透明度` 滑块补偿到 0.70
- `uInkDensity` 默认 0.22 和 `uInkSpread` 默认 0.18 会让墨核心很小，如果完全不可见可以微调
- 如需回滚，`git checkout` 恢复四个文件即可
